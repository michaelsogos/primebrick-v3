# AI AGENT INSTRUCTIONS - Primebrick Workspace

**IMPORTANT:** This is a meta-workspace. Read [AGENTS.md](./AGENTS.md) for complete AI agent instructions.

## Quick Start for AI Agents

- **Backend work:** Set working directory to `backend/` and read `backend/AGENTS.md`
- **Frontend work:** Set working directory to `frontend/` and read `frontend/AGENTS.md`
- **Never work from this root folder** - `.gitignore` excludes `backend/` and `frontend/`

## Repository Structure

- `backend/` - Primebrick API (independent Git repository)
- `frontend/` - Primebrick UI (independent Git repository)
- This directory - Meta workspace only (tooling, scripts)

See [AGENTS.md](./AGENTS.md) for complete instructions.
